import type { ReactNode } from "react";

/**
 * Displays the root of the non-primitives documentation section.
 * @returns A documentation section.
 */
export function NonPrimitivesRoot(): ReactNode {
    return (
        <div>
            The following functions create shapes using the 2D primitives above.
        </div>
    );
}
